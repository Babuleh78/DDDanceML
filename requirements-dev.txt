# Dev / CI tooling. Install with:  pip install -r requirements-dev.txt
ruff==0.6.9
mypy==1.11.2
pytest==8.3.3
pytest-cov==5.0.0
numpy==1.24.4   # pinned to match runtime, needed by unit tests
scipy==1.10.1   # pinned to match runtime, needed by dance_features tests
pydantic-settings>=2,<3   # config import in tests/conftest
